# EduSurvey - Quality Assurance & Testing Protocol

Defines static type checking (`npx tsc --noEmit`), Next.js linting (`npm run lint`), build compilation (`npm run build`), and manual test scenarios.
